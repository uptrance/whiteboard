{
  "name": "dom-ts",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "ts-node --esm src/index.ts",
    "build": "rollup -c rollup.config.js",
    "build:run": "pnpm run build -w -p @rollup/plugin-run",
    "test:unit": "vitest"
  },
  "devDependencies": {
    "@types/node": "^18.8.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.6.4",
    "vite": "^3.1.0",
    "vitest": "^0.24.0"
  }
}
